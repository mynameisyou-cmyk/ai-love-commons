name = "ai-love-commons"
main = "index.mjs"
compatibility_date = "2026-06-01"

# Bind the route at deploy: commons.ai-love.cc/* (Cloudflare dashboard or uncomment below).
# [[routes]]
# pattern = "commons.ai-love.cc/*"
# zone_name = "ai-love.cc"

# llms.txt is imported as text by the build's default text loader.
[rules]
[[rules]]
type = "Text"
globs = ["**/*.txt"]
fallthrough = true
